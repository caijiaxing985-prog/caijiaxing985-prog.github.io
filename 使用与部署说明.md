# Jianyoon 文案库管理版

这是已经做好的完整程序，包含公开文案库、登录后台、文案和图片编辑、草稿保存和发布。

## 你自己日常怎么用

1. 访问 `https://你的文案库域名/admin`，输入管理员密码。
2. 选择“评论 / 图文 / 图片”。选择已有条目编辑，或点“新增”。
3. 文案可以编辑正文和关键词；图片可上传、替换、改名称及介绍。用“上移 / 下移”调整顺序。
4. “保存草稿”只保存，不影响公开页面；“发布更新”才对外生效。
5. 朋友继续访问公开首页复制或下载。新打开页面立即读取已发布内容，已打开页面约 15 秒内同步，切回浏览器时也会刷新。

图片支持 PNG、JPG、WebP、GIF，单张不超过 15 MB。删除内容会先从草稿移除，发布后才从公开列表消失。为方便恢复和备份，图片文件不自动物理清理。

后台直接管理内容；以后不必改 Excel，也不必每次重新上传网站。若仍用 Excel 整理，可另行导入；当前版本没有 Excel 自动同步功能。

## 朋友首次部署：Docker 方式（推荐）

需要一台持续运行的服务器、Docker 和 Compose v2、可访问的独立子域名。个人电脑关机会导致网站无法访问，推荐部署在现有中转站服务器上。

1. 将本完整包解压到独立目录 `/srv/jianyoon`，不要覆盖原中转站。
2. 复制 `.env.example` 为 `.env`，填写：
   - `PUBLIC_ORIGIN=https://实际子域名`，必须与浏览器访问的协议和域名一致，不带路径。
   - `ADMIN_PASSWORD`：设置一个全新的、至少 12 个字符的强密码。没有默认密码。建议生成随机长密码，仅私下交给用户。
3. 在该目录运行：

```sh
docker compose -p jianyoon-library up -d --build
docker compose -p jianyoon-library ps
curl -f http://127.0.0.1:8787/api/health
```

4. 在现有 Nginx / 宝塔 / 1Panel 建立独立子域名站点，HTTPS 反向代理到 `http://127.0.0.1:8787`。示例见 `deploy/nginx.example.conf`。必须代理整个子域名，包括 `/api/`、`/uploads/` 和 `/admin`。若代理也运行在 Docker 中，使用同一容器网络的服务名访问，不能把代理容器自身的 127.0.0.1 当宿主机。
5. 开启该子域名的 HTTPS，关闭该站 HTML 与 API 的 CDN 缓存。上传限制至少 16 MB。不必对公网开放 8787 端口。
6. 用实际域名打开首页和 `/admin`，完成下面的验收。

`.env` 仅放服务器，不提交 GitHub。首次初始化后密码以加盐散列保存在数据库，后台可修改密码；再改 ADMIN_PASSWORD 不会覆盖已设置的密码。

## 如果不用 Docker

需要 Node.js 24 LTS（最低可用版本 22.13），不需要安装 MySQL，也不需要在服务器安装 npm 依赖。包里有编译好的 dist。

复制并填写 `.env` 后：

```sh
node --env-file=.env server/server.mjs
```

由管理员设置进程托管、开机启动，并把 HTTPS 子域名反向代理到 127.0.0.1:8787。建议将 DATA_DIR 设为独立绝对路径。GitHub 自动更新脚本默认适用于 Docker；非 Docker 部署需管理员按实际进程管理器调整脚本。

## 数据不会随程序更新丢失

- Docker 数据在固定命名卷 `jianyoon-library-data`，不在容器或程序目录内。
- 直接运行 Node 时数据在 DATA_DIR：包括 library.sqlite、其 WAL/SHM 文件及 uploads。
- server/seed.json 只在首次没有数据库时导入。程序更新不覆盖你在后台维护的内容。
- 不要删除数据卷，不要执行 `docker compose down -v`。
- 使用独立子域名根目录，不能把 dist 当成普通静态网站直接上传来获得后台功能。

## 备份和恢复

推荐管理员将命名卷纳入现有定时备份。完整备份前短暂停止本服务，用 tar 备份整个卷，再启动；不要只复制正在写入的 library.sqlite 而漏掉 WAL。
自动更新脚本会先停止本服务并备份数据卷到 `/srv/jianyoon/backups/`，之后启动新版本；原程序启动失败时恢复旧镜像。当前数据格式固定，不做破坏性迁移。未来涉及数据库结构变化需单独验证恢复。

恢复应先停止本服务，保留一份当前卷快照，再由管理员将所选备份还原至同一命名卷，保持原有 UID/GID 权限。之后启动并检查内容。不要触碰原中转站卷。备份包含账号散列和图片，限制访问并按现有策略保留。

## GitHub 自动更新（首次配置一次）

内容更新不需要这一节；修 bug 和改程序才需要。代码仓库：`caijiaxing985-prog/caijiaxing985-prog.github.io`，管理版使用独立分支 `codex/admin-library`。main 分支保留旧静态站。不要把本分支合并到 main 来运行 GitHub Pages，Pages 无法运行后台。

本包已提供 `.github/workflows/deploy-server.yml` 和 `deploy/update-server.sh`。管理员只需接通一次：

1. 按上面的 Docker 流程在 `/srv/jianyoon` 成功首次部署。确保 Linux 有 Python 3、curl、tar、flock、Docker 和 Compose v2。
2. 把 `deploy/update-server.sh` 安装到 `/srv/jianyoon/update-server.sh`，设置为 root 所有、权限 0755。`/srv/jianyoon`、compose.yaml、.env 也由管理员拥有，部署账号不能任意改写。脚本和路径按此配置配套。
3. 创建专用 SSH 部署用户及专用密钥；不要使用服务器 root 的日常密钥。管理员为部署用户配置 sudo，仅允许无密码执行 `/srv/jianyoon/update-server.sh`，不要给通配 sudo 权限或加入 docker 组。
4. 在该密钥的 authorized_keys 行加限制：

```text
restrict,command="sudo -n /srv/jianyoon/update-server.sh" ssh-ed25519 此处替换成专用公钥
```

5. 在 GitHub 仓库创建 Environment `library-server`，填写 Secrets：
   - DEPLOY_HOST：SSH 主机地址（确保 GitHub 执行器可到达）。
   - DEPLOY_PORT：SSH 端口，默认 22。
   - DEPLOY_USER：专用部署用户名。
   - DEPLOY_KEY：专用私钥，保存在 GitHub Secrets，不能写入代码或聊天。
   - DEPLOY_KNOWN_HOSTS：经管理员通过可信渠道核验过指纹的 known_hosts 内容（非 22 端口需要对应的 `[主机]:端口` 条目）。不要关闭主机校验。
6. 添加仓库 Variable `ENABLE_SERVER_DEPLOY=true`。在 Environment 限制可部署分支为 `codex/admin-library`；按团队要求保护分支和维护权限。
7. Actions 中运行 `Deploy managed library`，选择 `codex/admin-library`。验收一次：构建、测试、SSH 更新和健康检查均成功。之后我在这个分支推送程序修复，便可自动更新，保留数据。

代码更新期间会有短暂停机以备份数据。新镜像先构建再切换，启动失败自动尝试恢复旧镜像；历史镜像、release 目录和备份不自动删除，由管理员定期清理。镜像回滚不是所有业务 bug 的自动检测，发布后仍需验收核心操作。

自动更新权限意味着这个受信任仓库的维护者可以运行该应用的新代码，并访问文案库自己的数据。限定维护人员和部署分支。本程序不需要原中转站 API Key 或数据库访问权限。

## 部署验收

1. 无痕窗口打开公开首页，有初始 35 评论、35 图文、15 图片及直接展示的教程图片。
2. 未登录直接访问 /api/admin/draft 返回 401；公开页能浏览和复制。
3. 登录后新增测试评论，保存草稿；无痕窗口看不到它。
4. 发布后无痕窗口刷新看到它；重新编辑、排序、发布仍正确。
5. 上传图片，填名称和介绍，发布后可显示、查看原图、下载。
6. 删除测试条目并发布，公开列表移除。
7. 重启服务后原文案、管理员密码、图片仍存在。
8. 接通 GitHub 后推送一次无害更新，确认数据保留；查看 Actions 日志和健康检查。
9. 确认原中转站访问、登录和 API 不受影响；在真实微信设备测试链接、复制、长按保存。

完成后回传：首页网址、后台网址、部署目录和方式、GitHub 自动更新测试结果、备份方式。管理员密码请单独私下交付。

## 已验证与限制

本地 Node.js 已运行，后端接口流程及浏览器操作已验证。运行时只依赖 Node 内置模块。
本机未安装 Docker，也没有你朋友服务器连接：Docker 启动、远程反向代理、证书和 SSH 自动更新必须由首次部署方实机验收。配置模板的域名与 Secrets 需由管理员填入。
自有域名和 HTTPS 不保证微信永不提示风险。分享封面元信息已提供，但微信卡片样式取决于微信识别能力。
